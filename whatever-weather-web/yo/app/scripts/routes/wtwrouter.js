/*global define*/

define([
    'jquery',
    'backbone'
], function ($, Backbone) {
    'use strict';

    var WtwRouter = Backbone.Router.extend({
        routes: {
        }

    });

    return WtwRouter;
});
